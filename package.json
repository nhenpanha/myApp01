{
  "name": "myapp01",
  "version": "1.0.0",
  "description": "Minimal app for ECS Fargate test",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": { "node": ">=20" }
}

